[build]
  command = "npm install --force && npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "20.10.0"
  NPM_FLAGS = "--force"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200